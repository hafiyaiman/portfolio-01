import { GentingDriftGame } from "@/components/game/GentingDriftGame";

export const metadata = {
  title: "Genting Drift | Malaysian Touge",
  description: "A misty Malaysian mountain drift playground with four-wheel raycast physics.",
};

export default function GamePage() {
  return <GentingDriftGame />;
}
